# Secrets
.env

# Internal self-assessment, never published
project_grading.md

# Local, machine-specific Claude Code settings
.claude/settings.local.json

# Virtual environment
env/
venv/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Generated RAG index (rebuildable from data/store_policies.csv)
data/chroma_db/

# Benchmark run artifacts (large, regenerable from configs); traces are committed on purpose
bench/results/
bench/figures/

# Personal walkthrough notes for the benchmark study: kept on this branch only
# and deliberately never committed (they are prep notes, not a deliverable).
docs/walkthrough/
